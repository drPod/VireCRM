import { WaveTwoStub } from "~/components/layout/wave-two-stub";

export function meta() {
  return [{ title: "ESIs — genesisxsx" }];
}

export default function ESIs() {
  return <WaveTwoStub title="ESIs" />;
}
